{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset' } }) }}
    {{ form_widget(form) }}
    <div>
        <button class="btn btn-primary" >{# <span class="glyphicon glyphicon-log-in" ></span> #}{{ 'resetting.reset.submit'|trans }}</button>
    </div>
{{ form_end(form) }}
